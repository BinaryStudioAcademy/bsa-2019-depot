version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/bsa-2019-depot
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
hooks:
  BeforeInstall:
    - location: bin/before-install.sh
  ApplicationStart:
    - location: bin/app-start.sh
      runas: ubuntu